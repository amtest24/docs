# Getting Started

1. Create a branch (e.g., `docs/init`).
2. Edit files under `docs/` or `mkdocs.yml`.
3. Run `mkdocs build --strict` locally to validate.
4. Open a PR and merge to `main` to deploy via GitHub Actions.
