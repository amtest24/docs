```markdown
# docs— Documentation repository

This repository contains documentation for the `amtest24/docs` project, managed as "docs-as-code" using MkDocs (Material theme).

Quick start (local)
1. python -m venv .venv
2. source .venv/bin/activate
3. pip install -r requirements.txt
4. mkdocs serve
```